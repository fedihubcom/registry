{{#*inline "page"}}
<div class="container">
  <h1>Users</h1>

  <ul>
    {{#each users}}
      <li>{{ this.username }}, {{ this.encrypted_password }}</li>
    {{/each}}
  </ul>
</div>
{{/inline}}
{{~> (parent)~}}
